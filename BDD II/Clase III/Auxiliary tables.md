In SQL, auxiliary tables (sometimes called "helper," "utility," or "reference" tables) are tables that don't necessarily represent a core business entity (like a User or a Flight) but exist to simplify complex queries, improve performance, or enforce data integrity.

We often use them when updating the "amount" of a certain entity within our relational schema using [[Triggers]].