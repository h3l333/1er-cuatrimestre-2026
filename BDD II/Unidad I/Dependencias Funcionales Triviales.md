Son satisfechas por todas las relaciones. Por ejemplo, A -> A (el atributo A determina a A).
Una dependencia funcional es trivial si A -> B

⊆ : Indicates that one set is either a subset of another *or* equal to it.

>A -> B is trivial if B ⊆ A